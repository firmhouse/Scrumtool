%h1= @team.name

%h2 Team

%ul
  - @team.users.each do |user|
    %li
      = user.name
      &lt;#{user.email}>
      - if user.twitter_screen_name
        (@#{link_to user.twitter_screen_name, "http://twitter.com/#{user.twitter_screen_name}"})
  
%h2 Invite your team

%p
  Send them this link:
  = link_to invite_url(:id => @team.invite_code)